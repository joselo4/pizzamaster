/**
 * Libro PECOSAS: Fuente de verdad en BD (trigger movements -> transactions).
 * Este helper se desactiva (NO-OP) para evitar duplicados.
 */
export async function logPecosaTransaction(_params: any) {
  return;
}
