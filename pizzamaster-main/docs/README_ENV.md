# Entorno requerido

Crea un archivo `.env.local` con este formato:

```env
VITE_SUPABASE_URL=https://TU-PROYECTO-REAL.supabase.co
VITE_SUPABASE_KEY=TU_ANON_KEY_REAL
VITE_SUPABASE_ANON_KEY=TU_ANON_KEY_REAL
VITE_SENTRY_DSN=
```

Luego reinicia Vite con `npm run dev`.
