# Defaults solicitados

- Costo de envío por defecto: **0.00**
- Tiempo estimado por defecto: **25 min**
- Tab inicial: **Promo** (en /pedido)

Además, `delivery_fee = 0` ahora **sí se respeta** (se usa `??` y no `||`).
